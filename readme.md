# Current Progress

- Made Global EEGEnv module (contains electrode sim, feature field and SH compression (deterministic))
- Go into `sh_det_exp` directory and read the readme file to run api example script, and uncomment or modify lines as you please
- No learnable implementation yet 



- all python scripts ending with `_R.py` means its runnable as `python -m <path>.<to>.<script>` from the root directory and with no .py at the end 